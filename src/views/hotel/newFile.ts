/* __placeholder__ */
export default (await import('vue')).defineComponent({
    data() {
        return {
            data() {
                form: {
                    name: '';
                }
            }
        };
    },
    computed: {
        // displayProfile() {
        //   return `My name is ${this.name} and i am ${this.age}`
        // }
    },
    methods: {},
    mounted() { }
});
